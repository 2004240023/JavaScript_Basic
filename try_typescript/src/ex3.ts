type MyInfo = { name: string, age: number } ;
const newMyInfo: MyInfo = { name: "My Name", age: 20 };

type Profile = { name:String, age: number, like:String };
const newProfile: Profile = { name: "My Name", age: 21, like:"eating" };

console.log(newMyInfo);
console.log(newProfile);